import { CaslAbilityFactoryTs } from './casl-ability.factory';

describe('CaslAbilityFactoryTs', () => {
  it('should be defined', () => {
    expect(new CaslAbilityFactoryTs()).toBeDefined();
  });
});
