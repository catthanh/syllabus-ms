export enum CourseType {
  COMPULSORY = 0,
  OPTIONAL = 1,
}

export enum TeachingMethodEnum {
  LECTURE = 0,
  LAB = 1,
  SEMINAR = 2,
  TUTORIAL = 3,
  PROJECT = 4,
  FIELD_WORK = 5,
  OTHER = 6,
}

export enum TeachingLocationEnum {
  OFFLINE = 0,
  ONLINE = 1,
}

export enum AssessmentMethod {
  ATTENDANCE = 0,
  WEEKLY_ASSIGNMENT = 1,
  GROUP_ASSIGNMENT = 2,
  EXAM = 3,
  INVIDUAL_ASSIGNMENT = 4,
  GRADE_COMPLETION = 5,
}

export enum SyllabusStatusEnum {
  UPDATING,
  REQUESTED,
  APPROVED,
}
